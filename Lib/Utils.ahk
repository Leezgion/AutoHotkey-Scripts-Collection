; =================================================
; 📦 Utils.ahk - 通用工具函数
; =================================================

; -------------------------------------------------
; GetFileName - 从路径获取文件名
; -------------------------------------------------
GetFileName(path) {
    name := ""
    SplitPath(path, &name)
    return name
}

; -------------------------------------------------
; ShowNotification - 显示通知提示
; 参数: title - 标题
;       text  - 内容
;       duration - 显示时长(毫秒)，默认2000
; -------------------------------------------------
ShowNotification(title, text, duration := 2000) {
    ToolTip(title "`n" text)
    SetTimer(() => ToolTip(), -duration)
}
